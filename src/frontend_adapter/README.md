# oas2moon frontend adapter

OpenAPI 3.0.x JSON/YAML frontend for `oas2moon`.

This package parses supported documents, records the project-owned Frontend Model,
and validates the supported OpenAPI profile before the Canonical Client IR is built.

Repository: <https://github.com/Atman-Angle/oas2moon>
